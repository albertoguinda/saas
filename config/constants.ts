/**
 * Maximum number of projects allowed for users on the Free plan.
 */
export const FREE_PROJECT_LIMIT = 1;

/**
 * Duration in days for the automatic free trial.
 */
export const TRIAL_DURATION_DAYS = 7;

/**
 * Time to live for cached public pages (seconds).
 */
export const SITE_CACHE_TTL = 3600;

/**
 * Time to live for cached dashboard data (seconds).
 */
export const PANEL_CACHE_TTL = 600;
